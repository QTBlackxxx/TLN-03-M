---
- name: Configuracion OSPF core MPLS
  hosts: MPLS_routers
  gather_facts: no
  tasks:

    - name: Habilitar OSPF proceso 1
      cisco.ios.ios_config:
        lines:
          - router-id {{ loopback_ip }}
          - network 0.0.0.0 255.255.255.255 area 0
        parents: router ospf 1
